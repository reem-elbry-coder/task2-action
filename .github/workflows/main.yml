on:
  push:
    branches:
      - master







jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
    
      - name: "login to doker hub"
        run: docker login --username reemelbry --password dckr_pat_oyFzhtSj9_64cMgPtj0UloTRpSU
      

      